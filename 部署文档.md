# EduPilot 部署文档（阿里云 ECS）

> 适用于 Windows 本地 → Ubuntu 22.04 阿里云 ECS 的部署与更新

---

## 一、服务器信息

| 项目 | 值 |
|------|-----|
| 公网 IP | `182.92.202.78` |
| 操作系统 | Ubuntu 22.04 64位 |
| 实例规格 | 2核4G（ecs.e-c1m2.large） |
| SSH 端口 | 22 |
| 网站端口 | 80 |
| 登录用户 | `root` |
| 登录密码 | `Zhang1012`（如忘记，去阿里云控制台「重置实例密码」） |
| 项目路径 | `/home/ubuntu/EduPilot` |
| 虚拟环境 | `/home/ubuntu/EduPilot/venv` |
| Django 目录 | `/home/ubuntu/EduPilot/edupilot_project` |
| Gunicorn 配置 | `/home/ubuntu/EduPilot/deploy/gunicorn.conf.py` |
| systemd 服务 | `edupilot.service` |

---

## 二、前置准备（只需做一次）

### 2.1 阿里云安全组开放端口

阿里云控制台 → 云服务器 ECS → 实例 → 网络与安全组 → 安全组 → 入方向 → 手动添加：

| 协议 | 端口 | 授权对象 | 说明 |
|------|------|----------|------|
| TCP | 22 | 0.0.0.0/0 | SSH 登录 |
| TCP | 80 | 0.0.0.0/0 | 网站访问 |

### 2.2 本机安装 Python 依赖

```powershell
pip install paramiko
```

（paramiko 用于通过 SSH 密码登录自动执行部署命令，Windows PowerShell 不自带 sshpass）

---

## 三、完整部署流程（首次部署或大更新）

### 3.1 确认本地项目结构

```
EduPilot/
├── edupilot_project/          # Django 项目
│   ├── manage.py
│   ├── edupilot_project/      # settings.py / urls.py / wsgi.py
│   ├── chat/                  # 主应用
│   ├── templates/index.html
│   └── db.sqlite3
├── edupilot_agent/            # AI Agent 模块
├── deploy/                    # 部署配置
│   ├── deploy.sh
│   ├── gunicorn.conf.py
│   ├── nginx.conf
│   └── edupilot.service
├── requirements.txt
└── users.json
```

### 3.2 确认 requirements.txt 版本正确

**关键**：Django 6 尚未正式发布，必须用 5.x 稳定版：

```text
django>=5.1,<6
gunicorn>=22.0,<24
waitress>=2.1.2,<4
python-dotenv>=1.0.0
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.0
langchain-openai>=0.1.0
faiss-cpu>=1.7.0
openai>=1.0.0
tiktoken>=0.4.0
requests>=2.31.0
numpy>=1.24.0
Pillow>=10.0.0
```

### 3.3 确认 gunicorn.conf.py 绑定 0.0.0.0:80

`deploy/gunicorn.conf.py` 内容：

```python
import multiprocessing, os

chdir = '/home/ubuntu/EduPilot/edupilot_project'
wsgi_app = 'edupilot_project.wsgi:application'
bind = '0.0.0.0:80'           # 直接绑 80，不依赖 nginx
workers = 3
threads = 2
timeout = 120                  # AI 问答响应较慢，给 2 分钟
max_requests = 1000
max_requests_jitter = 50
accesslog = '/home/ubuntu/EduPilot/logs/gunicorn_access.log'
errorlog = '/home/ubuntu/EduPilot/logs/gunicorn_error.log'
loglevel = 'info'
proc_name = 'edupilot'
preload_app = False
```

### 3.4 一键部署脚本

在本地项目根目录创建 `deploy_to_ecs.py`，内容如下：

```python
# -*- coding: utf-8 -*-
""" EduPilot 上传到阿里云 ECS 的一键部署脚本 """
import paramiko, tarfile, os, sys
from pathlib import Path

HOST = '182.92.202.78'
PORT = 22
USER = 'root'
PASS = 'Zhang1012'

PROJECT_ROOT = Path(__file__).parent
LOCAL_PARENT = PROJECT_ROOT.parent
LOCAL_PKG = LOCAL_PARENT / 'EduPilot-deploy.tar.gz'
REMOTE_PKG = '/tmp/EduPilot-deploy.tar.gz'
REMOTE_DEPLOY_DIR = '/home/ubuntu/EduPilot'

def log(msg, tag='INFO'):
    print(f'[{tag}] {msg}')

def make_tarfile():
    if LOCAL_PKG.exists():
        LOCAL_PKG.unlink()
    excludes = ['__pycache__', '.pyc', '.bat', 'app.log',
                '.design', '.git', '.gitignore', 'venv']
    log(f'开始打包 → {LOCAL_PKG.name}')
    with tarfile.open(LOCAL_PKG, 'w:gz') as tar:
        for root, dirs, files in os.walk(PROJECT_ROOT):
            dirs[:] = [d for d in dirs if not any(ex in d for ex in excludes)]
            for f in files:
                if any(ex in f for ex in excludes):
                    continue
                full = Path(root) / f
                arcname = str(full.relative_to(LOCAL_PARENT)).replace('\\', '/')
                tar.add(full, arcname=arcname)
    log(f'打包完成：{LOCAL_PKG.stat().st_size / (1024*1024):.1f} MB')

def connect_ssh(timeout=15):
    client = paramiko.SSHClient()
    client.set_missing_host_key_policy(paramiko.AutoAddPolicy())
    client.connect(HOST, port=PORT, username=USER, password=PASS, timeout=timeout)
    log('SSH 连接成功')
    return client

def upload_scp(client, local_path, remote_path):
    log(f'SCP 上传 {local_path.name} → {remote_path}')
    sftp = client.open_sftp()
    sftp.put(str(local_path), remote_path)
    sftp.close()
    log('上传完成')

def run_cmd(client, cmd, timeout=600):
    log(f'执行：{cmd}', tag='SSH')
    stdin, stdout, stderr = client.exec_command(cmd, timeout=timeout, get_pty=True)
    while True:
        if stdout.channel.recv_ready():
            sys.stdout.write(stdout.channel.recv(16384).decode('utf-8','replace'))
            sys.stdout.flush()
        if stdout.channel.recv_stderr_ready():
            sys.stderr.write(stdout.channel.recv_stderr(16384).decode('utf-8','replace'))
            sys.stderr.flush()
        if stdout.channel.exit_status_ready():
            break
    return stdout.channel.recv_exit_status()

def main():
    # 1. SSH 连通测试
    client = connect_ssh()
    run_cmd(client, 'uname -a && df -h / | tail -1')
    client.close()

    # 2. 本地打包
    make_tarfile()

    # 3. SCP 上传
    client = connect_ssh()
    upload_scp(client, LOCAL_PKG, REMOTE_PKG)

    # 4. 服务器端：解压 + 装依赖 + migrate + 重启
    run_cmd(client, f'''
rm -rf {REMOTE_DEPLOY_DIR}
mkdir -p /home/ubuntu
cd /home/ubuntu
tar -xzf {REMOTE_PKG}
mkdir -p {REMOTE_DEPLOY_DIR}/logs
''')

    # 装依赖（用 venv 绝对路径，不切用户）
    run_cmd(client, '''
VENV=/home/ubuntu/EduPilot/venv
# 如果 venv 不存在就创建
if [ ! -d "$VENV" ]; then
    python3 -m venv $VENV
fi
# 配置清华源
mkdir -p /root/.pip
cat > /root/.pip/pip.conf <<'EOF'
[global]
index-url = https://pypi.tuna.tsinghua.edu.cn/simple
trusted-host = pypi.tuna.tsinghua.edu.cn
timeout = 180
EOF
$VENV/bin/python -m pip install --upgrade pip
$VENV/bin/pip install -r /home/ubuntu/EduPilot/requirements.txt
''', timeout=1800)

    # migrate + collectstatic
    run_cmd(client, '''
cd /home/ubuntu/EduPilot/edupilot_project
VENV=/home/ubuntu/EduPilot/venv
DJANGO_SETTINGS_MODULE=edupilot_project.settings_prod $VENV/bin/python manage.py migrate --noinput
DJANGO_SETTINGS_MODULE=edupilot_project.settings_prod $VENV/bin/python manage.py collectstatic --noinput
''', timeout=300)

    # 写 gunicorn.conf.py（确保绑 80）
    run_cmd(client, '''
cat > /home/ubuntu/EduPilot/deploy/gunicorn.conf.py <<'EOF'
import multiprocessing, os
chdir = "/home/ubuntu/EduPilot/edupilot_project"
wsgi_app = "edupilot_project.wsgi:application"
bind = "0.0.0.0:80"
workers = 3
threads = 2
timeout = 120
max_requests = 1000
max_requests_jitter = 50
accesslog = "/home/ubuntu/EduPilot/logs/gunicorn_access.log"
errorlog = "/home/ubuntu/EduPilot/logs/gunicorn_error.log"
loglevel = "info"
proc_name = "edupilot"
preload_app = False
EOF
''')

    # 写 systemd 服务文件
    run_cmd(client, '''
cat > /etc/systemd/system/edupilot.service <<'EOF'
[Unit]
Description=EduPilot Django Application
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=/home/ubuntu/EduPilot/edupilot_project
Environment="DJANGO_SETTINGS_MODULE=edupilot_project.settings_prod"
Environment="PYTHONUNBUFFERED=1"
ExecStart=/home/ubuntu/EduPilot/venv/bin/gunicorn -c /home/ubuntu/EduPilot/deploy/gunicorn.conf.py
Restart=always
RestartSec=5
LimitNOFILE=65536
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target
EOF
systemctl daemon-reload
systemctl enable edupilot
systemctl restart edupilot
sleep 10
systemctl status edupilot --no-pager -l | head -15
ss -tlnp | grep ":80"
curl -s -o /dev/null -w "HTTP %{http_code}\\n" http://127.0.0.1:80/
''', timeout=60)

    client.close()
    log(f'\n部署完成！浏览器打开：http://{HOST}', tag='DONE')

if __name__ == '__main__':
    main()
```

### 3.5 执行部署

```powershell
cd C:\Users\pp101\Desktop\智能体项目\EduPilot
python deploy_to_ecs.py
```

### 3.6 验证

浏览器打开 `http://182.92.202.78`，看到登录页（标题「EduPilot - 智能学业规划助手」）即成功。

---

## 四、日常更新流程（改了代码后重新部署）

只需 3 步：

```powershell
# 1. 确认本地代码能正常运行
cd C:\Users\pp101\Desktop\智能体项目\EduPilot\edupilot_project
python manage.py runserver

# 2. 执行部署脚本（自动打包+上传+重启）
cd C:\Users\pp101\Desktop\智能体项目\EduPilot
python deploy_to_ecs.py

# 3. 浏览器验证
# 打开 http://182.92.202.78
```

---

## 五、常见问题排查

### 5.1 SSH 连不上

```powershell
# 测试连通性
python -c "import paramiko; c=paramiko.SSHClient(); c.set_missing_host_key_policy(paramiko.AutoAddPolicy()); c.connect('182.92.202.78', username='root', password='Zhang1012'); print('OK'); c.close()"
```

- 报 `Authentication failed`：密码错了，去阿里云控制台「重置实例密码」→ 重启实例
- 报 `Connection refused`：22 端口没开，去安全组加 TCP 22 规则
- 报 `Connection timed out`：服务器关机了，去控制台启动实例

### 5.2 网站打不开（HTTP 000）

SSH 登录服务器排查：

```bash
# 查看 edupilot 服务状态
systemctl status edupilot

# 如果服务挂了，重启
systemctl restart edupilot

# 查看错误日志
tail -50 /home/ubuntu/EduPilot/logs/gunicorn_error.log

# 检查 80 端口占用
ss -tlnp | grep ":80"
```

### 5.3 pip 安装失败 `No matching distribution found`

原因：Django 版本号写错（如 `django>=6.0.0`，但 Django 6 未发布）。

修复：确认 `requirements.txt` 里是 `django>=5.1,<6`，然后重新部署。

### 5.4 页面报 500 内部错误

```bash
# 查看实时日志
journalctl -u edupilot -f

# 或者看 gunicorn 错误日志
tail -100 /home/ubuntu/EduPilot/logs/gunicorn_error.log
```

### 5.5 数据库需要迁移

```bash
cd /home/ubuntu/EduPilot/edupilot_project
VENV=/home/ubuntu/EduPilot/venv
DJANGO_SETTINGS_MODULE=edupilot_project.settings_prod $VENV/bin/python manage.py migrate
systemctl restart edupilot
```

---

## 六、手动 SSH 登录服务器操作

如果脚本出问题，可以手动登录：

```powershell
# Windows PowerShell 直接用密码 SSH（需装 OpenSSH 客户端）
ssh root@182.92.202.78
# 输入密码：Zhang1012
```

常用命令：

```bash
# 重启服务
systemctl restart edupilot

# 查看服务状态
systemctl status edupilot

# 查看实时日志
journalctl -u edupilot -f

# 查看项目文件
ls -la /home/ubuntu/EduPilot/

# 查看数据库
cd /home/ubuntu/EduPilot/edupilot_project
/home/ubuntu/EduPilot/venv/bin/python manage.py dbshell

# 手动装依赖
/home/ubuntu/EduPilot/venv/bin/pip install <包名>
```

---

## 七、本地开发部署

### 7.1 开发模式（改代码自动热重载）

双击 `启动项目.bat`，或手动：

```powershell
cd C:\Users\pp101\Desktop\智能体项目\EduPilot\edupilot_project
python manage.py runserver
```

浏览器打开 `http://127.0.0.1:8000`

### 7.2 稳定模式（Waitress 多线程）

双击 `生产部署(本地Waitress).bat`，或手动：

```powershell
cd C:\Users\pp101\Desktop\智能体项目\EduPilot\edupilot_project
pip install waitress
waitress-serve --listen=127.0.0.1:8000 edupilot_project.wsgi:application
```

---

## 八、架构说明

```
用户浏览器
    │
    ▼ HTTP:80
┌──────────────────────────────┐
│  阿里云 ECS 182.92.202.78     │
│  Ubuntu 22.04 / 2核4G         │
│                              │
│  ┌────────────────────────┐  │
│  │ systemd: edupilot.service│  │
│  │   ↓                     │  │
│  │ Gunicorn (3 workers)    │  │
│  │   bind: 0.0.0.0:80      │  │
│  │   ↓                     │  │
│  │ Django 5.2 (WSGI)       │  │
│  │   settings_prod.py      │  │
│  │   ↓                     │  │
│  │ SQLite (db.sqlite3)     │  │
│  │ LangChain + FAISS       │  │
│  │ OpenAI API              │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

**为什么不用 Nginx？**  
Gunicorn 直接绑 80 端口对外服务，省去 Nginx 反向代理层。2核4G 小规格实例下减少一层转发开销，够用。如后续需要 HTTPS / 静态文件加速 / 多站点，再加 Nginx。
